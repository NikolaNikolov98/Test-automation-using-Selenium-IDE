I use Selenium IDE to automate and validate core user flows within the application. The recorded test cases cover essential navigation paths and functional scenarios to verify that the application behaves correctly under typical user interactions.

The purpose of these automated tests is to ensure that key functionalities are accessible, stable, and performing as expected for both:

Unregistered users (guest access scenarios)

Registered users (authenticated user flows)
